apiVersion: apps/v1
kind: Deployment
metadata:
  name: python-service
  namespace: default
spec:
  replicas: 1
  selector:
    matchLabels:
      app: python-service
  template:
    metadata:
      labels:
        app: python-service
    spec:
      imagePullSecrets:
        - name: regcred
      containers:
        - name: python-service
          image: docker.io/gregor2323/akka-railway-crossing-service:latest
          imagePullPolicy: Always
          ports:
            - name: http
              containerPort: 8000
              protocol: TCP
---
apiVersion: v1
kind: Service
metadata:
  labels:
    app: python-service
  name: python-service-service
  namespace: default
spec:
  type: ClusterIP
  ports:
    - name: http
      port: 8000
      targetPort: 8000
  selector:
    app: python-service